# railway-track-defect-detection
A simple deep learning based web application to detect defects in railway track images using a trained CNN model. The backend is built with FastAPI and the frontend is developed using Streamlit.
